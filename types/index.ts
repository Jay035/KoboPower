export interface Disco {
  id: string;
  name: string;
  logo: string; // URL or Initials for this demo
  color: string;
}

export interface Benefit {
  title: string;
  description: string;
  icon: string;
}